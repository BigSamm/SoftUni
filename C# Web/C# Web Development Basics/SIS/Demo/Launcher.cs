namespace Demo
{
    using SIS.Framework;
    using SIS.Framework.Routers;
    using SIS.WebServer;

    public class Launcher
    {
        static void Main()
        {
            //var werServer = new WebServer(80, new ControllerRouter());

            //MvcEngine.Run(werServer);
        }
    }
}